# CML3112 — Assignment A2: Proof of Life

**Student:** 24/1035/BSCE-S
**Course:** CML3112

## What this program does
This notebook checks that my setup is working correctly. It:
- Confirms my Student ID is filled in
- Checks that basic Python math runs correctly (2 + 2)
- Shows what happens when a cell is run more than once without restarting
- Reads data from a CSV file to confirm the program can access outside data

## Files in this repository
- `24_1035_BSCE-S_W02.ipynb` — the main notebook (this program)
- `site_inspection_log.csv` — the supplied data file
- `README.md` — this file

## How to run it
1. Open `24_1035_BSCE-S_W02.ipynb` in Google Colab.
2. Upload `site_inspection_log.csv` using the Files sidebar in Colab.
3. Run all cells from top to bottom (Runtime → Restart session and run all).
4. Expected outputs: `4` (proof_sum), `17` (readings), `4` (CSV rows read).